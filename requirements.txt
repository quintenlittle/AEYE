fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
psutil>=5.9          # live CPU / RAM / network meters (GPU via nvidia-smi)
pywebview>=5.0       # desktop window (aeye.bat) -- uses Windows' WebView2
miniupnpc>=2.2       # P2P UPnP NAT traversal (optional -- falls back to LAN-only)
